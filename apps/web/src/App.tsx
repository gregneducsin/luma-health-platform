// Routing/pages land in Phase 7 (dashboard build-out). This placeholder keeps
// the package buildable/typecheckable through the earlier phases.
export default function App() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-sm text-gray-500">Luma Health — dashboard under construction.</p>
    </div>
  );
}
